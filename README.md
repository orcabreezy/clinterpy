# clinterpy

Interactive Command Line Interface (CLI) Library written in Python.

## Development

```bash
python -m unittest discover -s tests
```

## Package layout

- `src/clinterpy`: package source
- `tests`: project tests
